@model System.Collections.Generic.IEnumerable<Emblishment_Management_System.Models.emsUserU>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<section class="content-header">
    <h1>
        User
        <small>User details</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> User</a></li>
        <li class="active">User details</li>
    </ol>
</section>

<section class="content">
    <div class="box box-default">
        <div class="box-header with-border">
            <h3 class="box-title">User Details</h3>

        </div>
        <div class="box-body">
            <div id="1stRow" class="row form-group">
                <div class="col-md-2 col-lg-2">
                    @Html.ActionLink("Create New User", "Create", null, new { @class = "btn btn-default btn-sm" })
                </div>
            </div>
            <div id="2ndRow" class="row form-group">
                <div class="col-md-12 col-lg-12">
                    <table class="table table-bordered table-hover table-striped">
                        <thead>
                        <tr>
                            <th></th>
                            <th>
                                EPF
                            </th>
                            <th>
                                Username
                            </th>
                            <th>
                                First Name
                            </th>
                            <th>
                                Last Name
                            </th>
                            <th>
                                Email
                            </th>
                            <th >
                                Tel No
                            </th >


                        </tr>
                        </thead>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "btn btn-warning btn-xs glyphicon glyphicon-edit", title = "Edit User" })
                                    @Html.ActionLink(" ", "UserRoles", new { id = item.Id, username = item.Username }, new { @class = "btn btn-info btn-xs glyphicon glyphicon-wrench", title = "Change Roles" })
                                    @Html.ActionLink(" ", "EditPassword", new { id = item.Id }, new { @class = "btn btn-danger btn-xs glyphicon glyphicon glyphicon-user", title = "Change Password" })
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Epf)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Username)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.FirstName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.LastName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Email)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Tel)
                                </td>

                            </tr>
                        }

                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

